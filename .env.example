# =============================================================================
# AXLE - MOBILE MECHANIC SERVICE
# Environment Variables Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (REQUIRED)
# Get these from your Supabase project settings at https://app.supabase.com
# -----------------------------------------------------------------------------

# Your Supabase project URL (ends with .supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (safe to expose to the client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# -----------------------------------------------------------------------------
# NEXT.JS CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------

# Next.js environment (development, production)
NODE_ENV=production

# App URL for redirects and webhooks
NEXT_PUBLIC_APP_URL=https://your-app-domain.com

# -----------------------------------------------------------------------------
# DEPLOYMENT SETTINGS (VERCEL/HOSTING)
# -----------------------------------------------------------------------------

# Vercel deployment URL (auto-generated by Vercel)
# VERCEL_URL=your-app.vercel.app

# Custom domain (if using)
# NEXT_PUBLIC_DOMAIN=your-custom-domain.com

# -----------------------------------------------------------------------------
# FEATURE FLAGS (OPTIONAL)
# -----------------------------------------------------------------------------

# Enable/disable specific features
# NEXT_PUBLIC_ENABLE_ANALYTICS=true
# NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# -----------------------------------------------------------------------------
# SECURITY (PRODUCTION ONLY)
# -----------------------------------------------------------------------------

# API rate limiting (if implemented)
# API_RATE_LIMIT=1000

# Session timeout (if custom implementation)
# SESSION_TIMEOUT=3600

# =============================================================================
# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file to .env.local for local development
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env.local or .env files to git
# 4. For Vercel deployment, add these in the Vercel dashboard
# 5. For other hosting, configure environment variables accordingly
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED FOR BASIC FUNCTIONALITY:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# ALL OTHER VARIABLES ARE OPTIONAL FOR MVP DEPLOYMENT
# -----------------------------------------------------------------------------
